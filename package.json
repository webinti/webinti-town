{
  "name": "webinti-space",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "build-avatars": "python3 scripts/build-avatars.py",
    "prepare-map": "python3 scripts/prepare-map.py",
    "prepare-map:watch": "python3 scripts/prepare-map.py --watch",
    "dev": "npm run prepare-map && concurrently -n server,client,map -c blue,green,magenta \"npm run dev:server\" \"npm run dev:client\" \"npm run prepare-map:watch\"",
    "build": "npm run build --workspace=client && npm run build --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
